# 判题服务的大模型配置（可选）
#
# 不写这个文件也行 —— 服务会先读项目里的 code/.env（和前面 17 章共用同一份）。
# 想给判题单独指定别的模型时，才需要在这里覆盖。改完要重启 judge_server.py。

OPENAI_API_KEY=sk-xxxxxxxxxxxx
OPENAI_BASE_URL=https://api.deepseek.com/v1
OPENAI_MODEL=deepseek-chat
