# Week 05 · RAG（上）：切片与检索

> **本周章节：** 第 08 章 RAG 全链路（上半：链路全景 / 加载清洗 / 切片 / 向量化 / 检索优化）
> **本周代码：** `code/ch08_rag.py`
> **配套专题：** [[02-Wiki/专题总结/04-RAG全链路]]、[[02-Wiki/面试题库/02-RAG高频面试题]]
> **预计投入：** 4 小时（第 08 章共 8 小时，本周走完"检索侧"）

---

## 一、本周目标

- [ ] 背出 RAG 完整链路 13 步：加载 → 清洗 → 切片 → 向量化 → 存储 → 改写 → 召回 → 融合 → 重排 → 组装 → 生成 → 校验 → 评估，并说清"排错先定位环节"
- [ ] 说清中文 `chunk_size` 为什么不能照搬英文经验（单位与信息密度都不同），能给出起始值 300~500 字并说明理由
- [ ] 对比四种切片策略（固定长度 / 按语义结构 / 父子切片 / 递归分隔符），说清各自适用场景与代价
- [ ] 说清向量检索与 BM25 各自擅长什么：向量擅长同义改写，BM25 在精确字符串上更强且**零依赖**
- [ ] 会用 RRF 做混合检索融合，并解释"为什么只用排名就能免疫分数量纲"
- [ ] 说清两阶段检索（召回 20 条 → 重排 4 条）的精度与成本权衡：bi-encoder 快但粗、cross-encoder 准但慢
- [ ] 跑通 `ch08_rag.py`：切片统计、三路检索对比全部打印，无需联网

**过关标准：** 给你一个"用户搜不到答案"的 bad case，你能在 3 分钟内判断它是**切片问题**还是**检索未召回**，并说出用什么指标（`Hit@5` / `Recall@K` / 块语义完整率）验证你的判断。

---

## 二、预习：先想三个问题

> 读正文之前先自己想一想，读完回来对照。

1. `chunk` 太大和太小分别会出什么问题？如果 push 到两个极端，模型的回答会怎么坏掉？
2. 中文的 `chunk_size` 为什么不能照搬英文经验？同样的 500 这个数字，中英文各代表多少信息？
3. 向量检索和 BM25 各自擅长什么？RRF 为什么比"把两路分数归一化后相加"更稳？

---

## 三、Day 1／Day 2 学习记录

### Day 1 · （日期）

**读了：** `docs/第08章` 第 1–3 节（完整链路、加载与清洗、切片三参数与四种策略）

**关键理解（用自己的话写，不要抄正文）：**

**跑代码：**
```bash
AGENT_MOCK=1 python code/ch08_rag.py
```
- 切片统计（块数 / 平均长度 / 块语义完整率）关键几行：
- 和预期一致吗：

**没懂的地方：**

### Day 2 · （日期）

**读了：** `docs/第08章` 第 4–6 节（向量化与零依赖降级、向量库边界、Top-K 与混合检索 RRF）

**关键理解（用自己的话写，不要抄正文）：**

**跑代码：**
```bash
AGENT_MOCK=1 python code/ch08_rag.py
```
- 三路检索（BM25 / 向量 / RRF）的 `Hit@5` 与 MRR 数字：
- 找到几个"向量没召回但 BM25 召回"的 query：
- 和预期一致吗：

**没懂的地方：**

---

## 四、踩坑记录

| # | 现象 | 报错原文 | 原因 | 怎么解决的 | 花了多久 |
|---|------|----------|------|-----------|----------|
| 1 | | | | | |

> 填完记得把结论同步到 [[00-配置/进度看板]] 的「踩坑记录索引」。
> 本周高发坑：切片把表格/标题拦腰截断；`chunk_size` 直接抄英文教程的 1000；把"检索不到"当成"模型不行"；向量与相似度度量不匹配（用余弦却没做归一化）；BM25 需要分词但中文没分好。

---

## 五、动手改造

> 本周的「改一个地方」任务：改完要能预测结果，再跑验证。

**改造内容：** 切片对比实验——把 `chunk_size` 固定为 200 / 400 / 800 各跑一次 `mini_eval`，记录 `Hit@5` 与"块语义完整率"（抽 20 块人工看，数几块单独能读懂）。

**我的预测：**

**实际结果：**

**差异分析：** （不一致的话，是哪里理解错了？）

---

## 六、本周产出

**代码：** 可切换切片策略的索引构建脚本 + 三路检索对比脚本（BM25 / 向量 / RRF）

**笔记：** 一张三行切片对比表（`chunk_size` → `Hit@5` → 块语义完整率 → 结论与 token 成本关系）

**能用自己的话讲清的一个点：** 为什么"小块检索 + 大块喂模型"（父子切片）通常效果最好。

---

## 七、能力自检

| 能力 | 对应章节 | 状态 |
|------|----------|------|
| 说出 RAG 全链路并定位失败环节 | 08 | ✅ 掌握 / 🔄 半懂 / ❌ 不会 |
| 设计切片策略并做对比实验 | 08 | ✅ 掌握 / 🔄 半懂 / ❌ 不会 |
| 向量化选型与零依赖降级（TF-IDF / BM25） | 08 | ✅ 掌握 / 🔄 半懂 / ❌ 不会 |
| 混合检索与 RRF 融合 | 08 | ✅ 掌握 / 🔄 半懂 / ❌ 不会 |
| 两阶段检索（召回 + 重排）的取舍 | 08 | ✅ 掌握 / 🔄 半懂 / ❌ 不会 |

**三个自测题**（不看笔记回答）：

1. 为什么 chunk 不能太大也不能太小？分别会出什么问题？
2. 向量检索和 BM25 各自擅长什么？RRF 为什么比分数归一化更稳？
3. Rerank 为什么有效？为什么不能只用 cross-encoder 做检索？

---

## 八、遗留问题与下周计划

**遗留问题：**
-

**下周计划：** 第 08 章（下）：让答案带引用、引用机械校验、拒答降级、RAG 评估五指标与评测集

**AI 教练建议：**

---

> **填写说明：** 本文件由你（学员）在学完当周内容后填写。「关键理解」「踩坑记录」「动手改造」三栏**必须自己写**，其余可简短。
