# 代码审查报告 · 输出模板

> 这是 `skills/example-skill` 的输出形状模板。**步骤 4 / 5 的产物直接填进下面这张表**，
> 格式固定下来才能跨次对比（同一份 diff 两次审查的结果差异，是判断技能改好没改好的唯一依据）。

---

**审查范围：** `<改动来自哪里：git diff / 单个文件 / 片段；文件数>`　
**覆盖声明：** 本次覆盖 `<N>` 个文件、`<M>` 行改动；未覆盖：`<例如：超出 2000 行的大 diff 只审了前 800 行>`　
**工具结果：** `<scripts/count_diff.py --diff xxx.patch 的关键输出，如"合计 3 个文件：+128 / -14（共 142 行改动）">`

---

## 1. 问题清单

> 一行一个问题；风险等级只有三档：**高**（会出错 / 丢数据）、**中**（易踩坑）、**低**（风格）。
> "修复建议"要能直接照着改，不要写"建议优化一下"。

| 文件:行 | 风险等级 | 问题 | 修复建议 |
|---|---|---|---|
| `path/to/file.py:42` | 高 | | |
| `path/to/file.py:88` | 中 | | |
| `path/to/other.go:7` | 低 | | |

**按检查顺序过一遍（避免漏项）：** 正确性 → 边界条件 → 错误处理 → 并发与资源 → 安全 → 性能 → 可测性。

**若确实没有问题：** 写 `未发现高风险问题`，并注明本次检查覆盖了哪 7 个方向；**不能留空**。

---

## 2. 测试建议

> 每个**高风险**问题至少一条能复现的用例：给出输入、操作、期望结果。

| 针对问题 | 用例（输入 / 操作） | 期望结果 |
|---|---|---|
| #1 | | |

---

## 3. 未覆盖 / 待确认

> 显式声明本次没看的部分，以及需要作者确认的歧义。**没有这一节，读者会误以为你已经审完全部。**

- 未覆盖：
- 待确认：

---

## 4. 结论

**高风险问题数：** `<N>`　**中风险：** `<N>`　**低风险：** `<N>`
**建议动作：** `<合并 / 修改后合并 / 打回>`，理由一句话：

---

> **模板使用提醒：**
> 1. 表格里的每一行都要有风险等级——没有等级的清单等于没排序，作者不知道该先改哪个。
> 2. 不要顺手重写代码（那是编码任务）；需要示例时，只给**最小**替换片段。
> 3. 满足本模板全部小节即停止，不要额外扩写。
